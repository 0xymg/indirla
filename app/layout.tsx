import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Indirla - Video Downloader",
  description: "Download videos from YouTube, Twitter, Instagram, TikTok, and Facebook with ease. Fast, reliable, and free video downloader.",
  keywords: ["video downloader", "YouTube downloader", "Twitter downloader", "Instagram downloader", "TikTok downloader"],
  authors: [{ name: "Indirla" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
